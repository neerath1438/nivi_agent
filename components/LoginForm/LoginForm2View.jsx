import LoginForm2 from './LoginForm2';

const LoginForm2View = () => {
    return (
        <div style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            padding: '2rem',
            minHeight: '600px'
        }}>
            <LoginForm2 />
        </div>
    );
};

export default LoginForm2View;
